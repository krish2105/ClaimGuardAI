# --- LLM Provider ---
# If left blank, the pipeline runs in MOCK mode (deterministic rule-based
# stand-ins for each LLM call) so the whole system is demoable with zero cost
# and zero external dependency. Set a real key to use live Claude calls.
ANTHROPIC_API_KEY=
CLAUDE_MODEL_REASONING=claude-sonnet-4-5
CLAUDE_MODEL_EXTRACTION=claude-haiku-4-5

# --- Postgres ---
# Local dev default (matches docker-compose service "postgres")
DATABASE_URL=postgresql+psycopg2://claimguard:claimguard@localhost:5432/claimguard

# --- Qdrant ---
# If QDRANT_URL is unset, the app uses an embedded on-disk Qdrant under
# backend/data/ — no server required. Set QDRANT_URL to use Docker/Cloud.
# (QDRANT_LOCAL_PATH, FRAUD_MODEL_PATH, FRAUD_MODEL_META_PATH are NOT set
# here on purpose — the app computes correct absolute defaults under
# backend/ regardless of which directory you run it from. Only set these if
# you specifically want a non-default location.)
QDRANT_URL=
QDRANT_API_KEY=
QDRANT_COLLECTION=claimguard_policies

# --- App ---
APP_ENV=development
# Backend runs on 8100 and frontend on 3100 by default (not 8000/3000) to
# avoid clashing with other projects that commonly claim those ports.
CORS_ORIGINS=http://localhost:3100

# Set this to enable POST /admin/seed — a one-time HTTP bootstrap endpoint
# for hosts with no shell access (e.g. Render/Railway free tier). Leave
# blank to disable it entirely (the endpoint fails closed either way).
ADMIN_SEED_TOKEN=

# --- Frontend ---
NEXT_PUBLIC_API_BASE_URL=http://localhost:8100
NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8100
